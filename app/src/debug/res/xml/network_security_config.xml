<?xml version="1.0" encoding="utf-8"?>
<!--
    DEBUG-only network security config.
    Allows HTTP cleartext for local Laragon development.
    This file overrides src/main/res/xml/network_security_config.xml only in debug builds.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Laragon .test virtual host + subdomains (tenant domains) -->
        <domain includeSubdomains="true">hroes.test</domain>
        <!-- Android emulator maps host loopback to 10.0.2.2 -->
        <domain>10.0.2.2</domain>
        <domain>localhost</domain>
        <domain>127.0.0.1</domain>
    </domain-config>
</network-security-config>
